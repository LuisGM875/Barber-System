import { useState, type FormEvent } from "react";
import AuthLayout from "../../shared/layouts/authLayout";
import { forgotPassword } from "../services/authService";
export default function ForgotPasswordPage() { const [email, setEmail] = useState(""); const [message, setMessage] = useState<string | null>(null); const [loading, setLoading] = useState(false); const submit = async (event: FormEvent) => { event.preventDefault(); setLoading(true); try { setMessage((await forgotPassword(email)).message); } catch (err) { setMessage(err instanceof Error ? err.message : "No se pudo procesar la solicitud"); } finally { setLoading(false); } }; return <AuthLayout title="Recuperar contraseña" subtitle="Te enviaremos un enlace temporal" footerQuestion="¿Recordaste tu contraseña?" footerActionLabel="Inicia sesión" footerActionTo="/login">{message ? <p className="rounded-xl border border-green-500/20 bg-green-500/10 p-4 text-sm text-green-200">{message}</p> : <form onSubmit={submit}><label className="text-xs text-zinc-400">Correo electrónico<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-[#1C1C1C] px-4 py-3 outline-none" /></label><button disabled={loading} className="mt-5 w-full rounded-xl bg-[#C9A96E] py-3 text-sm font-semibold text-black">{loading ? "Enviando..." : "Enviar enlace"}</button></form>}</AuthLayout>; }
